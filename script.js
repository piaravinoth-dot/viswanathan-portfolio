const root=document.documentElement;document.addEventListener('keydown',e=>{if(e.key.toLowerCase()==='g')document.body.classList.toggle('show-grid')});
const dot=document.querySelector('.cursor-dot');if(dot){window.addEventListener('pointermove',e=>{dot.style.transform=`translate(${e.clientX}px,${e.clientY}px)`});document.querySelectorAll('a,.project-panel').forEach(el=>{el.addEventListener('mouseenter',()=>dot.classList.add('active'));el.addEventListener('mouseleave',()=>dot.classList.remove('active'))})}
const track=document.querySelector('.project-track');if(track){let down=false,startX,scroll;track.addEventListener('pointerdown',e=>{down=true;startX=e.pageX;scroll=track.scrollLeft;track.setPointerCapture(e.pointerId)});track.addEventListener('pointerup',()=>down=false);track.addEventListener('pointercancel',()=>down=false);track.addEventListener('pointermove',e=>{if(!down)return;e.preventDefault();track.scrollLeft=scroll-(e.pageX-startX)*1.15});track.addEventListener('wheel',e=>{if(Math.abs(e.deltaY)>Math.abs(e.deltaX)){track.scrollLeft+=e.deltaY;e.preventDefault()}},{passive:false})}